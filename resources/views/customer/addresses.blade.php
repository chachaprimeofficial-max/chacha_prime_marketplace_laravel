@extends('layouts.storefront') @section('title','Addresses') @section('content')<div class="dashboard-shell"><h1>Addresses</h1>@if(session('success'))<div class="notice success">{{session('success')}}</div>@endif<div class="panel">@foreach($addresses as $a)<div class="quick"><span><strong>{{$a->recipient_name}}</strong><br>{{$a->address_line1}}, {{$a->city}}, {{$a->country}}</span><small>{{$a->is_default?'Default':''}}</small></div>@endforeach</div><div class="panel"><h2>Add address</h2><form method="POST" action="{{route('customer.addresses.store')}}" style="display:grid;gap:12px">@csrf<input name="recipient_name" placeholder="Recipient name" required><input name="phone" placeholder="Phone"><input name="country" placeholder="Country" required><input name="state" placeholder="State"><input name="city" placeholder="City"><input name="postal_code" placeholder="Postal code"><input name="address_line1" placeholder="Address" required><label><input type="checkbox" name="is_default" value="1"> Default address</label><button class="button button-dark">Save Address</button></form></div></div>@endsection