@extends('layouts.storefront') @section('title','Wishlist') @section('content')<div class="dashboard-shell"><h1>Wishlist</h1><div class="panel">@forelse($items as $i)<div class="quick"><span><a href="{{route('product',$i->product_id)}}">{{$i->name}}</a><br>{{$i->retail_price}} {{$i->currency}}</span><form method="POST" action="{{route('customer.wishlist.remove',$i->product_id)}}">@csrf @method('DELETE')<button class="button">Remove</button></form></div>@empty<p>Your wishlist is empty.</p>@endforelse</div></div>@endsection