@extends('layouts.storefront') @section('title','Edit Product') @section('content')<div class="container"><h1>Edit Product</h1><div class="panel"><form method="POST" action="{{route('vendor.products.update',$product->id)}}" class="form-grid">@csrf<input name="name" value="{{$product->name}}" required><select name="category_id" required>@foreach($categories as $c)<option value="{{$c->id}}" @selected($c->id==$product->category_id)>{{$c->name}}</option>@endforeach</select><input name="price" type="number" step="0.01" value="{{$product->price}}" required><input name="stock_quantity" type="number" min="0" value="{{$product->stock_quantity}}" required><input name="sku" value="{{$product->sku}}"><textarea name="description">{{$product->description}}</textarea><button class="button">Save</button></form><form method="POST" action="{{route('vendor.products.delete',$product->id)}}">@csrf @method('DELETE')<button class="button">Delete Product</button></form></div></div>@endsection