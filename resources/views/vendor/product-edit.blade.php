@extends('layouts.storefront') @section('title','Edit Product') @section('content')<div class="dashboard-shell"><div class="panel"><h1>Edit Product</h1><form method="POST" action="{{route('vendor.products.update',$product->id)}}" class="form-grid">@csrf<input name="name" value="{{$product->name}}" required><select name="category_id"><option value="">Category</option>@foreach($categories as $c)<option value="{{$c->id}}" @selected($c->id==$product->category_id)>{{$c->name}}</option>@endforeach</select><input name="retail_price" type="number" step="0.01" value="{{$product->retail_price}}" required><input name="cost_price" type="number" step="0.01" value="{{$product->cost_price}}"><input name="currency" value="{{$product->currency}}" required><input name="stock" type="number" step="0.001" value="{{$product->stock}}" required><input name="sku" value="{{$product->sku}}" required><input name="short_description" value="{{$product->short_description}}"><textarea name="description">{{$product->description}}</textarea><button class="button button-dark">Save</button></form><form method="POST" action="{{route('vendor.products.delete',$product->id)}}" style="margin-top:12px">@csrf @method('DELETE')<button class="button">Archive Product</button></form></div></div>@endsection