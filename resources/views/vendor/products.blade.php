@extends('layouts.storefront')
@section('title','Vendor Products')
@section('content')<div class="container"><h1>Products</h1>@if(session('success'))<div class="notice">{{session('success')}}</div>@endif<div class="panel"><h2>Add Product</h2><form method="POST" action="{{route('vendor.products.store')}}" class="form-grid">@csrf<input name="name" placeholder="Product name" required><select name="category_id" required><option value="">Category</option>@foreach($categories as $c)<option value="{{$c->id}}">{{$c->name}}</option>@endforeach</select><input name="price" type="number" step="0.01" placeholder="Price" required><input name="stock_quantity" type="number" min="0" placeholder="Stock" required><input name="sku" placeholder="SKU"><textarea name="description" placeholder="Description"></textarea><button class="button">Submit for approval</button></form></div><div class="panel"><h2>Your Products</h2><div class="table-wrap"><table><tr><th>Name</th><th>SKU</th><th>Price</th><th>Stock</th><th>Status</th></tr>@foreach($products as $p)<tr><td>{{$p->name}}</td><td>{{$p->sku}}</td><td>{{$p->price}}</td><td>{{$p->stock_quantity}}</td><td>{{$p->status}}</td></tr>@endforeach</table></div>{{$products->links()}}</div></div>@endsection