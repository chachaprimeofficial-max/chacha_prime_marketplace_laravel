@extends('layouts.storefront') @section('title','Live Commerce') @section('content')<div class="dashboard-shell"><div class="panel"><h1>Live Commerce</h1>@if(session('success'))<div class="notice success">{{session('success')}}</div>@endif<form method="POST" action="{{route('vendor.live-commerce.store')}}" class="form-grid">@csrf<input name="title" placeholder="Stream title" required><input name="youtube_url" type="url" placeholder="YouTube URL" required><input name="youtube_video_id" placeholder="YouTube video ID (optional)"><select name="status"><option value="scheduled">scheduled</option><option value="live">live</option><option value="ended">ended</option></select><input name="starts_at" type="datetime-local"><button class="button button-dark">Save Stream</button></form></div><div class="panel table-wrap"><table><tr><th>Title</th><th>Status</th><th>Start</th><th>Video ID</th></tr>@foreach($streams as $s)<tr><td>{{$s->title}}</td><td>{{$s->status}}</td><td>{{$s->starts_at}}</td><td>{{$s->youtube_video_id}}</td></tr>@endforeach</table>{{$streams->links()}}</div></div>@endsection