@extends('layouts.storefront') @section('title','Shop') @section('content')<div class="container"><div class="dash-head"><div><span class="eyebrow">CHACHA PRIME</span><h1>Shop</h1></div><a class="button" href="{{route('cart')}}">Cart</a></div><form method="GET" class="panel"><input name="q" value="{{$q}}" placeholder="Search products..."><button class="button">Search</button></form><div class="product-grid">@forelse($products as $p)<article class="panel"><h3>{{$p->name}}</h3><p>{{Str::limit($p->description,100)}}</p><strong>{{$p->price}}</strong><div><a class="button" href="{{route('product',$p->id)}}">View</a></div></article>@empty<p>No published products found.</p>@endforelse</div>{{$products->links()}}</div>@endsection