@extends('layouts.storefront') @section('title',$product->name) @section('content')<div class="dashboard-shell"><div class="panel"><div class="eyebrow">PRODUCT</div><h1>{{$product->name}}</h1><p>{{$product->short_description ?: $product->description}}</p><div class="dashboard-card"><span>{{strtoupper($customerType)}} CUSTOMER PRICE</span><strong>{{$price}} {{$product->currency}}</strong><small>Base retail price: {{$product->retail_price}} {{$product->currency}}</small></div>@if($tiers->count())<div class="panel"><h3>Volume Pricing</h3>@foreach($tiers as $t)<div class="quick"><span>{{$t->min_quantity}}@if($t->max_quantity)–{{$t->max_quantity}}@else+@endif units</span><strong>{{$t->price}} {{$product->currency}}</strong></div>@endforeach</div>@endif<p>Stock: {{$product->stock}}</p><form method="POST" action="{{route('cart.add',$product->id)}}" style="display:flex;gap:12px;align-items:center">@csrf<input name="quantity" type="number" min="1" max="{{$product->stock}}" value="1"><button class="button button-dark">Add to Cart</button></form></div></div>@endsection