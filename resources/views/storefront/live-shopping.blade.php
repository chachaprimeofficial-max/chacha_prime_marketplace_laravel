@extends('layouts.storefront') @section('title','Live Shopping') @section('content')<div class="dashboard-shell"><div class="dashboard-hero"><div class="eyebrow">LIVE COMMERCE</div><h1>Live Shopping</h1><p>Watch seller streams and discover products in real time.</p></div><div class="dashboard-grid">@forelse($streams as $s)<div class="dashboard-card"><span>{{$s->status}}</span><h2>{{$s->title}}</h2><p>{{$s->business_name}}</p>@if($s->youtube_url)<a class="button button-dark" href="{{$s->youtube_url}}" target="_blank" rel="noopener">Watch Stream</a>@endif</div>@empty<p>No live shopping streams scheduled.</p>@endforelse</div>{{$streams->links()}}</div>@endsection