@extends('layouts.storefront') @section('title','Cart') @section('content')<div class="container"><h1>Your Cart</h1>@if(session('success'))<div class="notice">{{session('success')}}</div>@endif<div class="panel">@forelse($products as $p)<div class="quick"><span>{{$p->name}} × {{$cart[$p->id]}}</span><strong>{{$p->price*$cart[$p->id]}}</strong><form method="POST" action="{{route('cart.remove',$p->id)}}">@csrf<button class="button">Remove</button></form></div>@empty<p>Your cart is empty.</p>@endforelse<h2>Total: {{number_format($total,2)}}</h2>@if($products->count())<a class="button" href="{{route('auth.login')}}">Proceed to Checkout</a>@endif</div></div>@endsection